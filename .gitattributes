# Auto detect text files and perform LF normalization
* text=auto



:root {
    --primary: #eb1ccc;
    --secondary: #95989A;
    --text-color: #ffffff;
    --text-secundary-color: #000;
    --text-tertiary: #5d5d5d;
}

* {
    font-family: "Montserrat";
    margin: 0;
    padding: 0;
}

/* Event form */
#event-subscription {
    background: red
}
